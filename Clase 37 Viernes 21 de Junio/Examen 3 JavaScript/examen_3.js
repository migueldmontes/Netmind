// Variables
let type = document.querySelector(".type");
let size = document.querySelector(".size");
let document = document.querySelector(".document");
let checkbox = document.querySelector(".check_box");
let type_box = document.querySelector(".type");
let size_box = document.querySelector(".size_box");
let form_box3 = document.querySelector(".form_box");
let button = document.querySelector(".button");
let discount = document.querySelector("discount_input");
let counter = 0;
let cross = document.querySelector(".fa-times");


// Listeners
button.addEventListener("click", finalPrice);


// Functions

function sizePrice(size) {
    if (size == 4) {
        counter == 0;
        return typePrice 
    }
}

function finalPrice(e) {
    e preventDefault();
    
    error()
        if(deliveryPrice() > 0 {
    alert('El precio total es de ($deliveryPrice)(€ ');

    let order = document.createElement(p ');
    order.className = "order"
    order.innerHTML = 'Ha pedido una pizza ${type.value) $(size.value).<br>Precio estandar'$

}

function typePrice(e) {
    let errorType:

    if (type == 1) {

    }

}

function discountPrice() {
    if (discount.value = "firulais") {
        return sizePrice (size.value) *75 / 100;
    }
    else if (discount.value ||= "" ) {
        errorType = document.createElement("p");
        errorType = 
    }
}
function error() {
    if counter > 0 (
        let errorsArray = document.querySelectorAll(".errorsText");
        errorsArray = Array.from(errorsArray);

    )
}
function deliveryPrice {
    if (checkbox.checked) {
        return discountPrice() = 4;
    }
    else 

}

function printPrice(e) {
    e.preventDefault();

    error()

    if (checkbox.checked)
}

function popUp_appears() {
    pop_up.style.visibility
}